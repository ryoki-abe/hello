# Untited-Main.ipynb
## 採点ツール概要
・正解となるcsvファイルと比較するcsvファイルを比較し、比較結果を出力する。


## 使用方法
1. 正解データにcsvファイルまでのPathを書き込む。
2. 比較データにcsvファイルまでのPathを書き込む。(リスト形式であれば、複数のPathを書き込んでも良い)
3. データをソートするため、基準となるカラム名を書き込む。
4. csvファイルを出力する場所を、Pathで指定する。
追記：Pathは絶対パスでも相対パスでも良い。